# Copyright (C) 2024 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

# this file should be run in the root of the repo
services:
  text2csv:
    build:
      dockerfile: comps/text2csv/src/Dockerfile
    image: ${REGISTRY:-opea}/text2csv:${TAG:-latest}
